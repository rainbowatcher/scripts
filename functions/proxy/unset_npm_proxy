local npm_proxy
local npm_proxy=$(cat $HOME/.npmrc 2>/dev/null | grep '^proxy=')
if [[ -n $npm_proxy ]]; then
  sed -ie '/^proxy/d' "$HOME/.npmrc"
  sed -ie '/^https-proxy/d' "$HOME/.npmrc"
  judge "unset npm proxy"
else
  info "npm proxy has already unset"
fi
